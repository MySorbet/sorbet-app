export default interface UserType {
  id: string;
  firstName: string;
  lastName: string;
  accountId: string;
  email: string;
  bio: string;
  title: string;
  profileImage: string;
  tempLocation: string;
}
