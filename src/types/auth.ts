export interface SignUpWithEmailTypes {
  firstName: string;
  lastName: string;
  email: string;
  accountId: string;
  userType: string;
}

export interface SignInWithEmailTypes {
  email: string;
}
